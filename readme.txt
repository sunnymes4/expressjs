// Very first command to run is npm init it will create the package json file
// with all the initial configuration and main file which is index.js, first file to run
// whenever your app is deployed

1. npm init

// this will auto refresh your app/port for any change you make so you dont need to rerun
// it every time you make any change

2. npm i nodemon

// to run the server on local you need to run below command

3. npx nodemon index.js